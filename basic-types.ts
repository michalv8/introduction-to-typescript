// boolean 
const isLoading: boolean = true

// number
const amount: number = 123.456

// string 
const message: string = "Hello World"

// tuple
const index: [string, number] = ['foo', 123]

// array of numbers
const fibonacci: number[] = [0, 1, 1, 2, 3, 5, 8, 13]

// array of strings
const errors: string[] = ["undefined is not a function", "cannot compute"]

// enum
const enum Color { Red, Green, Blue }

// enum with explicit values
const enum HTTP {
    BAD_REQUEST = 400,
    NOT_FOUND = 404,
    INTERNAL_SERVER_ERROR = 500,
}
